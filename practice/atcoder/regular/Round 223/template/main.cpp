#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>

using namespace std;
typedef long long ll;

void task() {}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int T = 1;
    if (cin >> T) {
        while (T--) {
            task();
        }
    }
    return 0;
}
